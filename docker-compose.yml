services:
  postgres:
    image: postgres:16-alpine
    container_name: sina-learn-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sina_learn
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
